# 参与开发

zMatrix 是面向个人创作者的本地自媒体矩阵工作台，目前处于开发预览阶段。

## 本地检查

使用 Node.js 24，在项目根目录执行：

```sh
npm ci
npm run package
```

这会完成 TypeScript 检查、Vitest 测试、Edge 扩展构建和打包。网页预览使用 `npm run preview`；真实平台流程需要在 Edge 中加载 `.output/edge-mv3`。

## 平台适配

- 只操作用户明确选定的账号和稿件，最终发布由用户手动完成。
- 分别记录编辑器填充、素材上传、草稿重开、发布前设置和结果核实；模拟测试不替代真实验证。
- 未经核实的结果保留“待核实”或“发布设置待核对”，不自动重复提交。
- 不提交登录凭据、Cookie、私有稿件、账号标识或实际备份。`validation/` 用于仅保存在本机的验收资料。

## 数据兼容

保持扩展的公开 `key`、已有 IndexedDB 数据库名与备份格式兼容。迁移必须保留已保存稿件、素材和任务记录。`extension-identity.json` 只包含用于固定扩展 ID 的公钥。

## 许可证

本项目原创代码采用 MIT。涉及第三方或参考实现的改动，请保留相应许可和署名，参见 `THIRD_PARTY_NOTICES.md`。
